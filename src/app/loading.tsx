import { WildfiresSkeleton } from "@/components/wildfires-skeleton";

/**
 * Route-level loading UI for the home segment (shown during navigation).
 */
export default function Loading() {
  return <WildfiresSkeleton />;
}
